# This file contains localizations for the "en_US" locale. Since the default messages.properties already contains
# all the strings in english, we only need to specify the strings which are different in US english.

register.title=Welcome, buddy!